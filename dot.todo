* TODO [A] [33%] HBC K8S setup                                          :hbc:
** DONE [C] Open a way to connect to the k8s zk ensemble
   CLOSED: [2018-08-03 Fri 15:20]
   This way we can inspect the content of the zk ensemble and perform
   debug on (e.g.) kafka topics, etc.
** TODO [C] We need to the SSH into all worker nodes
** WIP [A] Use namespaces so we can isolate different stacks (e.g. needed for MySaks)
* Crypto
** TODO [A] Look at Steem.io (possible new addition for crypto portfolio?)
